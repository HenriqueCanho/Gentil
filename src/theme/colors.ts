// @ts-ignore
import tokens from './tokens';

export const COLORS = tokens as {
  bg: string;
  card: string;
  chip: string;
  accent: string;
  accentDim: string;
  text: string;
  muted: string;
  border: string;
  inputBg: string;
  error: string;
  overlay: string;
};
